# Core dependencies
crewai>=0.28.8
crewai-tools>=0.1.6
langchain-ibm>=0.1.0

# Vector database options (choose one or more)
chromadb>=0.4.15          # For ChromaDB vector store
faiss-cpu>=1.7.4          # For FAISS vector store (CPU version)
# faiss-gpu>=1.7.4        # For FAISS vector store (GPU version) - uncomment if using GPU
pinecone-client>=2.2.4    # For Pinecone vector store

# Embedding model options (choose one or more)
sentence-transformers>=2.2.2  # For local sentence transformer embeddings
openai>=1.3.0                 # For OpenAI embeddings (optional)

# Document processing
PyPDF2>=3.0.1             # For PDF processing
# pypdf>=3.0.1            # Alternative PDF library
python-docx>=0.8.11      # For DOCX processing

# Core ML and data processing
numpy>=1.21.0
pandas>=1.3.0
pydantic>=2.0.0

# Optional: Additional tools and utilities
python-dotenv>=0.19.0     # For environment variable management
tqdm>=4.62.0              # For progress bars during document processing